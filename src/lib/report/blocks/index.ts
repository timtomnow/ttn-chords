// Side-effect imports: registering every built-in report block. Import this
// module once (the editor and print view do) before reading the registry.
// Order here = palette order.

import './song';
import './text';
import './chordChart';
import './rhythm';
import './image';
import './spacer';
